# Minimal CI topology for cordelia-core#4 P2P replication e2e tests.
# 7 nodes: 2 boots, 2 edges, 2 keepers, 1 agent. Under 2GB RAM.
# Fast sync intervals (5s/15s) for quick convergence in CI.
#
# Usage:
#   cp topology-ci.env topology.env && bash gen-compose-zoned.sh

BACKBONE_COUNT=2
BACKBONE_PERSONAL=0
ORG_SPEC=alpha:1:1:1,bravo:1:1:0
IMAGE=cordelia-node:test
BEARER_TOKEN=test-token-fixed
RUST_LOG_LEVEL=warn
GOV_HOT_MIN=1
GOV_HOT_MAX=5
GOV_WARM_MIN=1
GOV_WARM_MAX=5
GOV_COLD_MAX=10
SYNC_MODERATE=5
SYNC_TACITURN=15
PROXY_ENABLED=0
PORTAL_ENABLED=0
